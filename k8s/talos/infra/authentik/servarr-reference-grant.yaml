apiVersion: gateway.networking.k8s.io/v1
kind: ReferenceGrant
metadata:
  name: servarr-to-authentik
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: servarr
  to:
    - group: ""
      kind: Service
      name: authentik-server

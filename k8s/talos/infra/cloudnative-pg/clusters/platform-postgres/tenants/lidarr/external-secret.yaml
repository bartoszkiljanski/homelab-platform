apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: lidarr-postgres
  annotations:
    argocd.argoproj.io/sync-wave: "15"
spec:
  refreshInterval: 1h

  secretStoreRef:
    name: bitwarden-secrets-manager
    kind: ClusterSecretStore

  target:
    name: lidarr-postgres
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: lidarr
        password: "{{ .password }}"

  data:
    - secretKey: password
      remoteRef:
        key: LIDARR_POSTGRES_PASSWORD
